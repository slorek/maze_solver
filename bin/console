#!/usr/bin/env ruby

require "bundler/setup"
require "maze_solver"

def fixture_path
  File.expand_path('../../spec/fixtures', __FILE__)
end

def fixture(file)
  File.read(File.join(fixture_path, file))
end

require "pry"
Pry.start

require "irb"
IRB.start
